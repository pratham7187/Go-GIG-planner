services:
  api:
    build: .
    image: gogig-vehicle-pipeline:latest
    ports:
      - "8000:8000"
    env_file:
      - path: .env
        required: false
    volumes:
      - ./uploads:/app/uploads
      - ./data:/app/data
    restart: unless-stopped

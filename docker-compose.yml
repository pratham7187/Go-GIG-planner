services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - path: .env
        required: false
    volumes:
      - ./uploads:/app/uploads
      - ./data:/app/data
    restart: unless-stopped
